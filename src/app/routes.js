export const isLocalHost = window.location.hostname === 'localhost'
export const isTasksHost = window.location.hostname === 'tasks.anothertasks.com'
export const isRootHost = window.location.hostname === 'anothertasks.com'
